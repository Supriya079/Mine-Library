<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.supriya.minelibrary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_lib"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_lib_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MineLibrary">
        <activity android:name=".UpdateActivity" android:exported="true"></activity>
        <activity android:name=".AddActivity" android:exported="true"/>
        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" android:exported="true">

        </activity>
    </application>

</manifest>